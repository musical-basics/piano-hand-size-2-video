# Raw footage is too large for normal GitHub pushes.
# Use Git LFS before tracking video media.
*.MOV
*.mov
*.MP4
*.mp4
*.wav
*.WAV

# Local transcription environment and caches.
venv_transcribe/
__pycache__/
*.pyc
.DS_Store
*.DS_Store

# Local secrets.
.env
.env.*
!.env.example

# Local generated voice/output metadata.
keyboard-trip/audio/voiceovers/cartesia_voice_clone_response.json

# Editor app is its own git repo with its own remote.
ai-agent-video-editor/

# Final Cut Pro library bundle internals (binary working state).
*.fcpbundle/

# Logic Pro narration project lives at the workspace root with its own
# version control; bounces get copied into keyboard-trip/audio/voiceovers/.
/Piano Hand Size Part 2 Audio Project/
/Piano Hand Size Part 2 Audio.wav
