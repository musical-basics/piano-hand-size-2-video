# Pass 7 V4 Fix Log

Output:

```text
review_cuts/piano_hand_size_part2_rough_cut_v4.mp4
```

Runtime:

```text
11:21
```

Specs:

```text
1280x720, 30 fps, H.264 video, AAC audio, 123 MB
```

## Changes Applied

- Extended `001_IMG_0256_0142am_trip_setup.MOV` from 8s to 16s in the cold open so the setup line no longer cuts off abruptly.
- Fine-edited `055_PICKUP_front_facing_intro.MOV` into two pieces:
  - `0:00-0:06`
  - `0:13.5-0:30.5`
- Removed the false start and repeated ending line from the front-facing intro.
- Replaced the early `010_IMG_0266_drive_broll_2.MOV` use with `004_IMG_0259_sheets_stop_middle_of_nowhere.MOV`, so IMG_0266 now only appears after the car-nap/recovery beat.
- Kept travel music under all VO travel montage sections and added low music under the cold-open placeholder/title cards.
- Kept `002_IMG_0257_hagerstown_gas_station.MOV` rotated 90 degrees clockwise.
- Kept every `018_IMG_0274_ds_size_lineup_on_steinway.MOV` use rotated 90 degrees counterclockwise.
- Kept `027_IMG_0283_athena_internals_reconnaissance.MOV` rotated 90 degrees counterclockwise.
- Kept the technical still inserts rotated 90 degrees counterclockwise.
- Extended the mileage beat by adding a 2s still hold after `044_IMG_0300_177777_mileage.MOV`.
- Kept `048_IMG_0304_hotel_car_broke_down.MOV` out of the cold open and later in the trip-fights-back section.

## Known Rough Edges

- Travel music is still a placeholder generated bed.
- Cartesia voiceovers are still temporary.
- P056 remains a placeholder until the hand/key comparison pickup is recorded.
- The front-facing intro is now cleaner, but it is still a coarse source-range edit rather than a full word-level editor pass.
